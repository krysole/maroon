--
-- Test program.
--

fn printf[string: [i8], ...]: i32;
fn puts[string: [i8]]: i32;
fn exit[exit_code: i32]: halt;

let a <- array{i64, 10}[];

fn sum[a: i64, b: i64, c: i64]: i64 {
  return a + b * c;
}

fn main[]: halt {
  printf["Hello world with sum[1, 2, 3] == 1 + 2 * 3 == %d.\n", sum[1, 2, 3]];
  
  exit[0];
}
