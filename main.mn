--
-- Test program.
--

fn puts[[char] string] i32;
fn exit[i32 exit_code] halts;

fn main[i32 argc, [char][] argv] halts {
  -- if not true and false {
  --   puts["Hello world.\n"];
  -- }
  
  let a <- 1 as i32;
  
  exit[0];
}
