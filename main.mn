--
-- Test program.
--

fn printf[[i8] string, ...] i32;
fn puts[[i8] string] i32;
fn exit[i32 exit_code] halt;

-- let a <- array{i64, 10}[];

fn main[] halt {
  let a <- i64[100];
  let p <- ptr[a];
  
  printf["Hello world with a == %d.\n", ref[p]];
  
  exit[0];
}
